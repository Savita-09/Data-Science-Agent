# Public backend URL, without credentials. Leave empty for the local combined app.
# Configure this in Vercel and redeploy, or enter a URL in the app's API connection.
VITE_API_BASE_URL=
# Never put APP_API_KEY, GROQ_API_KEY, or LLM_API_KEY in a VITE_ variable.
